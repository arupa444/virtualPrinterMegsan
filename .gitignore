# --- Never commit runtime data or secrets ---
# The live config holds real URLs / auth tokens; only the template is tracked.
config.json
# Simulator runtime data (received documents, SQLite db, admin token)
simulator/data/
received/
failed/
log.txt

# --- Python / venv ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.python-version

# --- Vendored binaries (users add these themselves) ---
vendor/*.dll
vendor/*.exe

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/
